#include <bits/stdc++.h>

using namespace std;
using ll = long long int;
template <typename T> using V = vector<T>;

#define dl "\n"

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    return 0;
}
